<section class="hero">
  <!-- Background gradient effect -->
  <div class="hero-bg-gradient"></div>
  
  <!-- Glowing accent in the background -->
  <div class="hero-glow-1"></div>
  <div class="hero-glow-2"></div>
  
  <div class="container">
    <div class="hero-content">
      <!-- Hero content -->
      <div class="hero-text">
        <h1>
          Create Viral <span class="text-mint">UGC</span> Content That Converts
        </h1>
        <p>
          Mint UGC helps brands create authentic user-generated content for Reels, YouTube, & TikTok that drives engagement and sales.
        </p>
        <div class="hero-buttons">
          <a href="#" class="button button-primary">
            Get Started
          </a>
          <a href="#features" class="button button-secondary">
            Learn More
          </a>
        </div>
      </div>
      
      <!-- Hero image/visual -->
      <div class="hero-visual">
        <div class="hero-frame">
          <!-- Frame/screen effect -->
          <div class="hero-screen">
            <!-- Content inside frame -->
            <div class="hero-screen-content">
              <div class="hero-logo">
                MINTUGC
              </div>
            </div>
          </div>
          
          <!-- Decorative elements -->
          <div class="hero-decoration-1"></div>
          <div class="hero-decoration-2"></div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="scroll-indicator">
    <span>Scroll to explore</span>
    <div class="scroll-mouse">
      <div class="scroll-dot"></div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background-color: #121212;
  }
  
  .hero-bg-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, #0A0A0A, #121212);
  }
  
  .hero-glow-1 {
    position: absolute;
    width: 24rem;
    height: 24rem;
    background-color: rgba(70, 255, 236, 0.1);
    border-radius: 50%;
    filter: blur(60px);
    bottom: -5rem;
    right: -5rem;
  }
  
  .hero-glow-2 {
    position: absolute;
    width: 16rem;
    height: 16rem;
    background-color: rgba(70, 255, 236, 0.05);
    border-radius: 50%;
    filter: blur(60px);
    top: -2.5rem;
    left: -2.5rem;
  }
  
  .container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
    position: relative;
    z-index: 10;
    box-sizing: border-box;
  }
  
  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
    gap: 3rem;
  }
  
  @media (min-width: 1024px) {
    .hero-content {
      flex-direction: row;
    }
  }
  
  .hero-text {
    text-align: center;
  }
  
  @media (min-width: 1024px) {
    .hero-text {
      width: 50%;
      text-align: left;
    }
  }
  
  h1 {
    font-size: 2.25rem;
    font-weight: bold;
    color: white;
    line-height: 1.2;
    margin: 0;
  }
  
  @media (min-width: 768px) {
    h1 {
      font-size: 3rem;
    }
  }
  
  @media (min-width: 1024px) {
    h1 {
      font-size: 3.75rem;
    }
  }
  
  .text-mint {
    color: #46FFEC;
  }
  
  p {
    margin-top: 1.5rem;
    font-size: 1.125rem;
    color: #d1d5db;
    max-width: 36rem;
  }
  
  @media (min-width: 768px) {
    p {
      font-size: 1.25rem;
    }
  }
  
  @media (min-width: 1024px) {
    p {
      margin-left: 0;
      margin-right: 0;
    }
  }
  
  .hero-buttons {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    justify-content: center;
  }
  
  @media (min-width: 640px) {
    .hero-buttons {
      flex-direction: row;
    }
  }
  
  @media (min-width: 1024px) {
    .hero-buttons {
      justify-content: flex-start;
    }
  }
  
  .button {
    padding: 0.75rem 2rem;
    font-weight: 500;
    border-radius: 0.5rem;
    text-align: center;
    text-decoration: none;
    transition: background-color 0.3s;
  }
  
  .button-primary {
    background-color: #46FFEC;
    color: #121212;
  }
  
  .button-primary:hover {
    background-color: #3ADBCA;
  }
  
  .button-secondary {
    background-color: #1A1A1A;
    color: white;
  }
  
  .button-secondary:hover {
    background-color: #333;
  }
  
  .hero-visual {
    position: relative;
  }
  
  @media (min-width: 1024px) {
    .hero-visual {
      width: 50%;
    }
  }
  
  .hero-frame {
    position: relative;
    width: 100%;
    max-width: 32rem;
    margin: 0 auto;
  }
  
  .hero-screen {
    width: 100%;
    aspect-ratio: 16 / 9;
    background-color: #0A0A0A;
    border-radius: 0.5rem;
    border: 1px solid rgba(70, 255, 236, 0.3);
    box-shadow: 0 4px 6px -1px rgba(70, 255, 236, 0.1);
    overflow: hidden;
    max-width: 100%;
  }
  
  .hero-screen-content {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(to bottom right, #1A1A1A, #0A0A0A);
  }
  
  .hero-logo {
    color: #46FFEC;
    font-size: 2.5rem;
    font-weight: bold;
    letter-spacing: 0.1em;
    max-width: 100%;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  
  @media (min-width: 768px) {
    .hero-logo {
      font-size: 3rem;
    }
  }
  
  .hero-decoration-1 {
    position: absolute;
    bottom: -1rem;
    right: -1rem;
    width: 6rem;
    height: 6rem;
    background-color: rgba(70, 255, 236, 0.1);
    border-radius: 50%;
    filter: blur(20px);
  }
  
  .hero-decoration-2 {
    position: absolute;
    top: -1rem;
    left: -1rem;
    width: 5rem;
    height: 5rem;
    background-color: rgba(70, 255, 236, 0.1);
    border-radius: 50%;
    filter: blur(20px);
  }
  
  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  .scroll-indicator span {
    color: rgba(70, 255, 236, 0.7);
    font-size: 0.875rem;
    margin-bottom: 0.5rem;
  }
  
  .scroll-mouse {
    width: 1.5rem;
    height: 2.5rem;
    border: 2px solid rgba(70, 255, 236, 0.3);
    border-radius: 9999px;
    display: flex;
    justify-content: center;
  }
  
  .scroll-dot {
    width: 0.375rem;
    height: 0.75rem;
    background-color: #46FFEC;
    border-radius: 9999px;
    margin-top: 0.5rem;
    animation: bounce 1s infinite;
  }
  
  @keyframes bounce {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(0.5rem);
    }
  }
</style> 